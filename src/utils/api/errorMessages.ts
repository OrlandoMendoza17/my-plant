const DUPLICATED_EMAIL_NOT_ALLOWED = 'duplicate key value violates unique constraint "Users_email_key"'
const NOT_FOUND_USER = "User not found!"

const errorMessages = {
  DUPLICATED_EMAIL_NOT_ALLOWED,
  NOT_FOUND_USER,
}

export default errorMessages;